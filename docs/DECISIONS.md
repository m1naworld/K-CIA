# K-CIA Lite — 의사결정 로그 (DECISIONS.md)

**작성일:** 2026-01-30

---

## DEC-001: 시나리오 1(P1 입지 Top3 추천) 우선 구현 전략

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-30 |
| **결정** | 8개 시나리오 중 시나리오 1(P1, 예비 창업자 — 입지 Top3 추천)을 1차 구현 대상으로 확정 |
| **근거** | (1) 핵심 UI 3종(3D Hex맵 + 사이드바 + 챗봇)을 모두 포함하여 플랫폼 뼈대를 세울 수 있음. (2) 필요 데이터셋(D1/D2/D3/D5/D9)이 다른 시나리오에도 공통 사용되어 재사용성이 높음. (3) "카페 창업"이라는 직관적 스토리로 데모/검증이 용이함 |
| **대안** | 시나리오 5(리스크 진단)를 먼저 → 그러나 UI 복잡도가 높고 리스크 레이어 추가 설계 필요. 시나리오 3(팝업)을 먼저 → 타겟 필터(연령/성별) 추가 작업 필요 |
| **영향범위** | M0~M4 전체 마일스톤 구성, 데이터 최소셋 선정, API/UI 설계 |
| **재검토 조건** | 시나리오 1 MVP 완료 후, 사용자 피드백으로 우선순위 재조정 |

---

## DEC-002: 행정동 + 상권코드 병행 구현 (Q1 반영)

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-30 |
| **결정** | `dim_area(area_type)` 이원화 + `bridge_area_h3_weight`로 양쪽 모두 H3 매핑. UI에서 "영역 기준(행정동/상권)" 토글 제공. 기본 프리셋은 행정동 4개 + 골목상권 20~40개 |
| **근거** | (1) D1/D5는 상권코드 기준, D2는 행정동 기준으로 제공되어 양쪽 지원이 필수. (2) 사용자에 따라 익숙한 기준이 다름(창업자=상권, 컨설턴트=행정동). (3) 공간조인으로 상호 매핑 가능하나 근사치임을 인지하고 UX에 표기 |
| **대안** | 상권코드만 지원 → D2(점포) 활용 어려움. 행정동만 지원 → D1/D5 정밀도 손실 |
| **영향범위** | dim_area 스키마, bridge 테이블, 모든 API의 `area_type` 파라미터, UI 토글 컴포넌트 |
| **재검토 조건** | 공간조인 오차가 사용자 혼란을 야기할 경우 → 한쪽 기본값 고정 + 다른 쪽은 "고급" 옵션으로 전환 |

---

## DEC-003: 시나리오 1 데이터 최소셋(Must) 확정

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-30 |
| **결정** | Must: D1(매출), D5(유동), D2(점포), D3(상권영역), D9(행정동 경계). Should: D11(실시간). Could: D4(상주인구), D8(집객시설) |
| **근거** | (1) 시나리오 1의 6단계(탐색→선택→질의→분석→결과→액션)에서 사용하는 데이터셋을 역추적. (2) D1+D5=맵 시각화 핵심, D2=경쟁 분석 핵심, D3+D9=공간 기준. (3) D11은 후행성 보정에 유용하나 없어도 시나리오 완결 가능. (4) D4/D8은 시나리오 2/3에서 주로 사용 |
| **대안** | D11을 Must로 올림 → 수집 복잡도 증가(실시간 스냅샷), 성수동 장소 존재 미확인 리스크 |
| **영향범위** | M1 전체 작업 범위, ETL 우선순위 |
| **재검토 조건** | D11 장소 확인 후 Should→Must 승격 검토. 시나리오 2 진입 시 D5 시간대 breakdown 검증 |

---

## DEC-004: H3 해상도 기본 res=10 (Q2 반영, 2026-02-04 변경)

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-04 (변경) |
| **결정** | H3 기본 해상도 res=10 사용. 성능 문제 시 res=9 / res=8 다운그레이드 가능 |
| **근거** | (1) res=10은 edge 약 66m로 건물 단위 정밀 분석에 적합. (2) 점포 기반 weight 계산 시 더 정확한 분포 반영. (3) 기존 bridge_area_h3_weight가 res=10으로 생성되어 일관성 유지 |
| **대안** | res=9 (edge 약 174m) → Hex 수 감소, 렌더링 빠름. res=8 → 더 가벼움 |
| **영향범위** | bridge_area_h3_weight 행 수, 맵 렌더링 성능, API 응답 크기 |
| **재검토 조건** | 맵 렌더링 > 3초 시 res=9 전환 검토 |

---

## DEC-005: 유튜브/소셜 모듈은 Phase 3 이후 (옵션)

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-30 |
| **결정** | 유튜브 모듈은 공공데이터 기반 시나리오 4개 이상 안정화 후(Phase 3 이후) 붙임. OFF 상태에서 모든 Q&A/추천이 공공데이터만으로 성립하도록 설계 |
| **근거** | (1) 확정사항: "유튜브/소셜은 옵션, OFF여도 성립". (2) 공공데이터 파이프라인 안정화가 우선. (3) 유튜브 H3 매핑 정밀도 낮음(지역 스코프 수준) |
| **대안** | Phase 1에서 동시 구현 → 범위 확대, 리스크 증가 |
| **영향범위** | Supervisor 라우터(Social Agent 비활성), Insight Agent 응답 템플릿("소셜 데이터 미포함" 문구) |
| **재검토 조건** | 사용자 피드백에서 "왜(원인)" 설명 요구가 높을 경우 Phase 2로 앞당김 |

---

## DEC-006: LLM 모델 선택 (GPT-4o-mini 시작)

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-30 |
| **결정** | SQL Agent와 Insight Agent 모두 GPT-4o-mini로 시작. 골든 쿼리 테스트에서 성공률 < 85% 시 GPT-4o로 에스컬레이션 |
| **근거** | (1) 비용 효율(4o-mini는 4o 대비 ~10배 저렴). (2) SQL 생성은 스키마가 단순(테이블 6~7개)하여 4o-mini로 충분할 가능성 높음. (3) 품질 부족 시 전환 경로가 명확(동일 API, 모델명만 변경) |
| **대안** | 처음부터 GPT-4o → 비용 부담 |
| **영향범위** | backend/agents/ 내 모델 설정, API 비용 |
| **재검토 조건** | M4 골든 쿼리 회귀 테스트 결과 |

---

## DEC-007: 콘텐츠 생성(4컷 만화/쇼츠)에 Gemini Nano Banana 사용

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-30 |
| **결정** | 시나리오 6(4컷/쇼츠 구조화)의 콘텐츠 생성에 Gemini Nano Banana를 사용. Phase 4에서 도입 |
| **근거** | (1) 4컷 만화/쇼츠는 텍스트+이미지 생성이 필요하며, Gemini Nano Banana가 이 용도에 적합. (2) 기존 GPT-4o는 Q&A/분석에 집중하고, 콘텐츠 생성은 별도 모델로 분리하여 역할 명확화. (3) 비용/성능 최적화 — 콘텐츠 생성은 분석과 다른 특성(멀티모달 출력)을 가짐 |
| **대안** | GPT-4o + DALL-E로 통일 → 모델 의존성 단일화되나 비용 증가. 텍스트만 생성(이미지 없음) → 설득력 저하 |
| **영향범위** | 시나리오 6 구현, backend/agents/에 Content Agent 추가, Gemini API 키 환경변수 추가 |
| **재검토 조건** | Phase 4 진입 시 Gemini Nano Banana API 안정성/품질 평가 후 확정 |

---

## DEC-008: open_cnt/close_cnt NULL 허용 임계값 80%

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-31 |
| **결정** | `fact_store_area_qtr.open_cnt/close_cnt`의 NULL 임계값을 5% → 80%로 완화 |
| **근거** | 서울 Open API D2(`VwsmAdstrdStorW`)가 대부분 업종에 대해 개폐업 수를 제공하지 않음 (75.6%/73.6% NULL). 구조적 결측이므로 데이터 품질 문제가 아님 |
| **대안** | 0으로 채움 → 잘못된 정보. 해당 컬럼 제거 → 존재하는 데이터까지 손실 |
| **영향범위** | `etl/quality/check_quality.py` 임계값 설정 |
| **재검토 조건** | 다른 API 소스에서 개폐업 데이터를 보충할 수 있을 경우 임계값 재강화 |

---

## DEC-009: D2 API 서비스명 및 행정동코드 수정

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-01-31 |
| **결정** | D2 점포-행정동 API 서비스명을 `VwsmStorCdTrdarAdong` → `VwsmAdstrdStorW`로 변경. dim_area 행정동코드를 `1104xxxx` → `1120xxxx` 체계로 변경 |
| **근거** | (1) 기존 서비스명 `VwsmStorCdTrdarAdong`은 서울 열린데이터광장에 존재하지 않는 잘못된 값이었음 (ERROR-500 반환). (2) 실제 OA-22172 서비스명은 `VwsmAdstrdStorW`로 확인. (3) API 응답의 행정동코드(`11200650` 등)와 DB 저장 코드(`11040650` 등)가 코드 체계가 달라 매칭 실패 — API 기준으로 통일 |
| **대안** | 뒤 4자리로 매칭 → 코드 체계 모호, 다른 구의 동과 충돌 위험 |
| **영향범위** | `etl/collectors/seoul_api_collector.py` (서비스명), `dim_area` 테이블 (area_code 4건), `etl/load_store_api.py` (SEONGSU_DONG_CODES) |
| **재검토 조건** | 다른 데이터셋(D1/D5 등)과 행정동코드 조인 시 체계 불일치 여부 확인 필요 |

---

## DEC-010: Chat API SSE 스트리밍 방식 채택

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-01 |
| **결정** | POST /api/chat은 SSE(Server-Sent Events) 방식으로 스트리밍. WebSocket은 사용하지 않음 |
| **근거** | (1) 챗봇은 요청-응답 패턴이므로 SSE가 적합 (서버→클라 단방향). (2) FastAPI StreamingResponse로 구현이 단순. (3) Vercel AI SDK가 SSE 네이티브 지원. (4) WebSocket은 양방향이 필요 없고 연결 관리 복잡도만 증가 |
| **대안** | WebSocket → 양방향 불필요, 인프라 복잡. 일반 JSON 응답 → 진행 단계 실시간 표시 불가 |
| **영향범위** | `backend/api/chat.py`, 프론트엔드 M3-4 챗봇 UI |
| **재검토 조건** | 멀티턴 대화(컨텍스트 유지)가 필요할 경우 WebSocket 재검토 |

---

## DEC-011: SQL Agent 업종명 매칭 이슈 → M4 골든 쿼리에서 해결

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-01 |
| **결정** | SQL Agent가 사용자 자연어("커피 전문점", "디저트 카페")를 DB 업종명("커피-음료")으로 매칭하지 못하는 이슈를 M4-1에서 해결. 현재 M2는 구조적 완결성(SSE 파이프라인) 우선 |
| **근거** | (1) dim_category에 59개 업종이 있으며, 사용자 표현과 DB명이 상이함. (2) 프롬프트에 업종 목록을 주입하거나, 2-step(먼저 LIKE 검색 → 매칭된 이름으로 본 쿼리) 방식 필요. (3) M4-1 골든 쿼리 3종과 함께 검증하는 것이 효율적 |
| **대안** | 지금 즉시 수정 → M2 범위 초과, 테스트 없이 수정 시 regression 위험 |
| **영향범위** | `backend/agents/sql_agent.py` 프롬프트 또는 로직 |
| **재검토 조건** | M4-1 착수 시 |

---

## DEC-012: HexMap 경계 필터링 및 상권 구역(zone) 시스템 도입

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-02 |
| **결정** | (1) 성수동 4개 행정동 외곽을 하나의 폴리곤으로 정의하여 경계 밖 헥사곤 제거. (2) 8개 상권 구역 폴리곤을 정의하고 각 헥사곤에 zone을 할당. 상권 외 지역은 `"residential"`로 회색 낮은 높이 처리. (3) 셀 클릭 시 동일 zone 전체를 하이라이트 |
| **근거** | (1) 기존 바운딩 박스 방식은 한강/뚝섬 등 성수동 밖 영역까지 포함하여 시각적 혼란. (2) 상권/주거 구분이 없으면 모든 셀이 동일한 색상 스케일로 표시되어 핵심 상권이 묻힘. (3) 개별 셀 클릭만으로는 상권 단위 파악이 어려움 → zone 단위 하이라이트로 공간적 맥락 제공 |
| **대안** | Turf.js 등 라이브러리 사용 → 번들 크기 증가, 간단한 ray-casting으로 충분. DB에서 zone 할당 → 현재 mock 데이터 단계에서는 클라이언트 처리가 효율적 |
| **영향범위** | `HexMap.tsx`, `mapStore.ts`, `map.ts` 타입 |
| **재검토 조건** | 실제 DB 데이터 연동 시 서버사이드 zone 할당으로 전환 검토. 경계 폴리곤 정밀도는 실제 SHP 데이터로 교체 가능 |

---

## DEC-013: 챗봇에 선택 상권 카드 컨텍스트 주입

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-03 |
| **결정** | 프론트에서 선택된 상권의 카드 데이터(`/api/map/hexagon/{h3}` 응답)를 `POST /api/chat`에 전달하여 LLM 컨텍스트로 사용 |
| **근거** | (1) 카드 데이터와 챗봇 응답의 일관성 확보. (2) 서버에서 동일 데이터를 재계산하는 중복 제거. (3) 사용자 질문이 "지금 선택한 곳"에 집중될 때 정확한 컨텍스트 제공 |
| **대안** | 서버에서 선택 상권 데이터를 다시 쿼리 → 구현 복잡도 증가, 데이터 불일치 가능 |
| **영향범위** | `frontend/src/hooks/useStreamingChat.ts`, `frontend/src/types/chat.ts`, `backend/api/chat.py`, `backend/api/schemas.py`, `backend/agents/insight_agent.py` |
| **재검토 조건** | 프론트 전송 페이로드가 과도하게 커질 경우 → 서버 조회 방식으로 전환 검토 |

---

## ~~DEC-014: 점포 수 기반 H3 Weight 도입~~ — 철회 (2026-02-07)

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-04 (철회: 2026-02-07) |
| **결정** | ~~소상공인 API 점포 좌표 기반 H3 weight 계산~~ → **불필요로 판단하여 삭제** |
| **철회 사유** | 면적 기반 weight로 충분. 추가 API 키 발급 및 유지보수 부담 대비 효과 미미 |

---

## DEC-015: Phase 2 건너뛰고 Phase 3 먼저 진행

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-07 |
| **결정** | S2+S5 (시간대+리스크) 이전에 S3+S4 (팝업+비교) 먼저 구현 |
| **근거** | (1) Phase 2 → Phase 3 순서 의존성 없음. (2) D5 인구통계 데이터(flow_by_demo) 이미 적재되어 S3 구현 가능. (3) 사용자 우선순위에서 S3+S4가 높음 |
| **대안** | 원래 순서(Phase 2 먼저) → 추가 시간 소요, 사용자 요구와 불일치 |
| **영향범위** | PLAN.md 확장 순서 변경, M6~M9 마일스톤 정의 |
| **재검토 조건** | Phase 3 완료 후 Phase 2 진행 |

---

## DEC-016: SNS 모듈 범위 확장 (YouTube → YouTube + Naver Blog)

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-07 |
| **결정** | 유튜브만 → 유튜브 + 네이버 블로그/카페로 확장 |
| **근거** | (1) 비용 0 기준: Naver Search API 무료(25K calls/day). (2) 한국 로컬 상권 데이터 최적 — 성수동 관련 콘텐츠는 네이버 블로그/카페에 압도적. (3) X/Instagram 제외: X Free tier 검색 불가, Instagram Graph API 비즈니스 계정 필요 |
| **대안** | YouTube만 → 한국 로컬 데이터 커버리지 부족 |
| **영향범위** | M9 마일스톤, ETL 2개 추가 (naver_collector, load_naver_trends), 환경변수 추가 |
| **재검토 조건** | Naver API 정책 변경 시 |

---

## DEC-017: SNS 데이터 H3 매핑 불가 → 지역 스코프 수준 시작

| 항목 | 내용 |
|------|------|
| **날짜** | 2026-02-07 |
| **결정** | `area_scope='seongsu'`로 성수동 전체 수준 집계 |
| **근거** | (1) SNS 콘텐츠에 정확한 좌표 없음 — 블로그/영상 제목에서 특정 H3로 매핑 불가. (2) PRD 지침: "H3 매핑 어려우면 지역 스코프로 먼저 제공". (3) 향후 NER(장소명 추출) 등으로 상권 수준 매핑 가능 |
| **대안** | 강제 매핑 시도 → 노이즈 증가, 신뢰도 저하 |
| **영향범위** | fact_social_trend_daily 스키마(area_scope 컬럼), Frontend (맵 오버레이 대신 배지/패널) |
| **재검토 조건** | NER/장소명 매핑 파이프라인 구축 시 상권 수준으로 업그레이드 |
